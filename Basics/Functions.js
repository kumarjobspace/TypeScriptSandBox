/*
function showTodos(todo: { title: string, text: string }) {
    console.log(todo.title + ' : ' + todo.text );
}

let myTodo = {title: 'Trash', text: 'Take out trash'};
showTodos(myTodo);

*/
function showTodos(todo) {
    console.log(todo.title + ' : ' + todo.text);
}
var myTodo = { title: 'Trash', text: 'Take this trash out' };
showTodos(myTodo);
